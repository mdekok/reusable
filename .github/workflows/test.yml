name: Test

on:
  workflow_dispatch:
    inputs:
      Environment:
        default: Test
        required: true
        type: choice
        options:
        - Development
        - Test
        - Production
  schedule:
    - cron: '0 1 * * *' # 01:00 UTC

env:
  Environment: ${{ github.event.inputs.Environment }}
  
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: echo $Environment
